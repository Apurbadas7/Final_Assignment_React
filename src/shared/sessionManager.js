import { log } from "./logger";

export const resetSessionTimer = () => {
    log("[Session] Resetting session timer.");
};

export const restartSession = () => {
    log("[Session] Restarting session.");
};
